package dayOne;

public class sample{
	public static void main(String[] args) {
		int i=-243;
		int j=243;
		if(i==j);
		System.out.println("numbers are the same");
	}
}

//output:numbers are the same
//reason:
//if condition is terminated immediately without a block
//so the output statement is always printed
